为所有章节建立悬浮目录映射（若需要启用 FloatingSectionNav），便于快速跳转。

列定义确认：表头是否为“月份 | 充 | 放 | 尖 | 峰 | 平 | 谷 | 深”？你给的样例里“充 放 充 放 尖 峰 平 谷 深”出现了两次“充/放”，是否笔误，还是想分别统计两个不同维度的“充/放”？如果是后者，请说明第二组“充/放”的含义。
- 分别统计两个维度的“充/放”，按时间顺序遍历从早到晚相同逻辑的时段内总电量，例如00:00-05:00为充，06:00-08:00为放，09:00-11:00为充，18:00-20:00为放，即第一组为00:00-05:00为充，06:00-08:00为放，第二组为09:00-11:00为充，18:00-20:00为放。如果23:00-00:00也为充，则与00:00-05:00合并，即为一个23:00-05:00第一个充的内容（跨日合并为当日数据）

单位与精度：默认用 kWh，保留 1 位小数是否可以？
- 可以

汇总年份：若数据包含多年的小时点，是只按“当前选择的年份”汇总，还是跨年合并？（建议跟随页面已有的年份选择）
- 跨年合并，相同月份的数据加一起

“运行逻辑=待机”是否不计入“充/放”列（只统计“充”“放”），确认一下。
- 只统计充、放

TOU 列顺序：按“尖、峰、平、谷、深”展示是否符合你期望？（项目当前 Excel 导出亦使用此顺序）
- 按“尖、峰、平、谷、深

缺失/清洗：已清洗的小时数据直接累计；无需再做额外插值对吧？
- 是的，已清洗的小时数据直接累计；无需再做额外插值

位置与交互：统计表放在日×时矩阵的下方是否合适？是否需要折叠/展开？
- 放在日×时矩阵的下方，需要能够折叠/展开

字段命名：表头需展示中文小标题“储能逻辑区间总用量汇总”“TOU时段用电量汇总”并做分组表头吗？
- 需要两个表头，“储能逻辑区间总用量汇总”“TOU时段用电量汇总”

过滤联动：是否需要按月份下拉选择器联动只显示某月合计？（你给的样例是 1–12 月全展示）
- 1–12 月全展示

是否可以增加点击其中一个月份，可以显示当月每日的统计数据