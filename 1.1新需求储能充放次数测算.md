怎么从另一个程序中移植主要功能过来这个程序，主要移植的是储能充放次数的计算、储能配置参数的输入、后续需要当前程序的前端界面新建页面进行展示计算结果
储能充放逻辑的配置可以使用当前的程序功能、用户负荷数据导入可以使用当前程序的功能
遍历参考程序文件夹：D:\Desktop\ai\1028负荷展示和tou配置\参考储能次数测算程序文件夹\1010_储能容量匹配度评估模型_claude，并遍历当前程序，请先拆分参考程序的程序逻辑和内容，拆分一个移植的任务方案，我们来讨论

---


1. 关键实现映射（落地方式）

策略构造（从格子到最多两次循环），选用：方案B：合并多段为两次（按时长/能量占比合并），精度更好

当前格子可能定义超过两次循环，可能出现在日末端类似23:00-00:00这种情况，如果出现跨日的情况且逻辑一致的话，需要将相同的日末与日处进行合并

2. 待您确认的问题

循环次数超过两次的处理方式优先选择哪种？（仅取前两次/合并成两次/阻断并提示）
- 当前格子可能定义超过两次循环，可能出现在日末端类似23:00-00:00这种情况，如果出现跨日的情况且逻辑一致的话，需要将相同的日末与日处进行合并

初期是否只支持 step_15min（SOC 逐点积分）口径，window_avg 作为后续选项？还是两者都要（通过开关）？
- 初期支持window_avg口径，step_15min（SOC 逐点积分）口径作为后续选项

TOU 电价是否完全由“月→档位→价格”与“日期规则”驱动？是否需要导入外部 JSON 的 TOU 方案（如参考程序的 tou_config_sample.json）？
-TOU 电价完全由当前程序的“月→档位→价格”与“日期规则”驱动，当前程序支持导入和前端配置

是否需要生成 Excel 报表与 TOU 配置快照？如需要，保存路径与下载机制如何约定（临时目录/静态服务）？
API 入参是否统一用 multipart（文件+JSON 一次提交）？还是先上传文件到 /api/load/analyze，拿到 token 再调用测算接口？
- 需要生成 Excel 报表与 TOU 配置快照，保存与其他要去你自行解决

SOC 跨日结转默认是否开启？若关闭，是否采用 initial_soc 作为每日起点（参考默认 0.1）？
- SOC 跨日结转默认关闭，采用 initial_soc 作为每日起点（参考默认 0.05）

能量公式选择（physics vs sample）默认用哪个？行业口径倾向 physics（物理一致）？
- 这个不是很明白请说的清晰些

先不要进行程序开发，我们还没讨论结束
请在根目录新建文件夹来保存“迁移任务方案（分阶段与交付物）”需求文档为markdown文件

---
建议推进步骤

第1日：确认问题清单 → 决定策略映射与 API 形态 → 后端数据契约冻结
第2-3日：后端落地（/api/storage/cycles + 算法迁移 + 简化 TOU/策略构造）与本地验证
第4日：前端新页与图表搭建（月/日 + 概览）
第5日：联调与边界处理（跨午夜、多循环、缺价/缺点）与文档