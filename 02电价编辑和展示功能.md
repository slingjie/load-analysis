我们已经完成了tou时段编辑功能，但是还想增加tou电价编辑和展示功能。
其中电价每个月的数据值一样的，电价数值和tou时段可以对应。
新增一个界面来进行电价编辑和展示，其中电价展示采用echart中的“阶梯折线图”图表，参考代码如下：

```
import * as echarts from 'echarts';

var chartDom = document.getElementById('main');
var myChart = echarts.init(chartDom);
var option;

option = {
  title: {
    text: 'Step Line'
  },
  tooltip: {
    trigger: 'axis'
  },
  legend: {
    data: ['Step Start', 'Step Middle', 'Step End']
  },
  grid: {
    left: '3%',
    right: '4%',
    bottom: '3%',
    containLabel: true
  },
  toolbox: {
    feature: {
      saveAsImage: {}
    }
  },
  xAxis: {
    type: 'category',
    data: ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun']
  },
  yAxis: {
    type: 'value'
  },
  series: [
    {
      name: 'Step Start',
      type: 'line',
      step: 'start',
      data: [120, 132, 101, 134, 90, 230, 210]
    },
    {
      name: 'Step Middle',
      type: 'line',
      step: 'middle',
      data: [220, 282, 201, 234, 290, 430, 410]
    },
    {
      name: 'Step End',
      type: 'line',
      step: 'end',
      data: [450, 432, 401, 454, 590, 530, 510]
    }
  ]
};

option && myChart.setOption(option);

```

方案B：按月定价（推荐）— 易实现、可表达季节差异；表单为 12×5，复杂度适中。

电价维度：采用“按月 × TOU 档”，需要多月份统一修改的功能

默认值与单位：元/kWh、默认价（深/谷/平/峰/尖）的参考数值：
深：默认为空
谷：0.3000元/kWh
平：0.6000元/kWh
峰：0.9000元/kWh
尖：默认为空

精度与校验：小数位数4 位），不需要最小/最大范围限制

导入导出：导出、同步支持导入覆盖

展示增强：需要在阶梯折线背景按 TOU 档着色，并在折线上显示电价数值

与日期规则：目前仅按“月度默认规则”映射电价，增加需要选择某个“日期规则”进行电价曲线展示