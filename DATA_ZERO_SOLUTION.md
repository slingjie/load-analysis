# ⚠️ 数据为0的真实原因 - 完整操作指南

## 🔍 问题确认（通过浏览器调试发现）

您的 "Project Summary" 页面显示：

```
数据状态检查:
○ 负荷数据未上传        ← 这就是为什么全是0！
○ 质量报告未生成
○ 储能测算未完成
```

**结论**: 您没有上传负荷数据，也没有运行储能测算，所以报告全是0是**完全正常的行为**！

## ✅ 为什么诊断显示"系统正常"？

诊断脚本测试的是：
- ✅ 后端服务能否运行
- ✅ API能否处理空数据（返回0）
- ✅ API能否处理完整数据（返回实际数值）

但诊断**无法检测您是否真的上传了数据**！

就像：
- ✅ 汽车能启动（系统正常）
- ❌ 油箱是空的（您没上传数据）
- ❓ 为什么车不走？→ 因为没油！

## 🎯 正确操作流程（手把手教学）

### 第1步：上传负荷数据 ⏱️ 2分钟

1. 打开浏览器 http://localhost:5173/
2. 点击页面上方的 **"上传负荷文件"** 大按钮
3. 选择测试数据，例如：
   ```
   负荷测试数据\中恒新材料负荷数据 展示用.csv
   ```
4. 等待上传完成（进度条100%）
5. 看到成功提示

**检查点**: 刷新页面，应该看到负荷曲线图和统计数据

### 第2步：查看负荷分析结果 ⏱️ 1分钟

1. 点击导航栏的 **"Load Analysis"** 按钮
2. 确认看到：
   - 📈 负荷曲线图表
   - 📊 统计数据：
     ```
     平均负荷：XXX kW
     峰值负荷：XXX kW
     谷值负荷：XXX kW
     ```
   - 📅 数据日期范围

**检查点**: 这些数值不应该是0

### 第3步：配置并运行储能测算 ⏱️ 3分钟

1. 点击导航栏的 **"Storage Cycles"** 按钮
2. 填写储能参数（使用这些测试值）：
   ```
   储能容量（kWh）: 4000
   储能功率（kW）:  2000
   充电效率:       0.9
   放电效率:       0.9
   SOC 最小值:     0.1  (10%)
   SOC 最大值:     0.9  (90%)
   充电余量:       0.1  (10%)
   放电余量:       0.1  (10%)
   ```
3. 点击 **"Calculate"** 按钮
4. 等待计算完成（10-30秒）
5. 看到结果显示：
   ```
   年等效循环次数: XXX 次/年
   首年收益:      XXX 万元
   ```

**检查点**: 这些数值不应该是0

### 第4步：生成项目评估报告 ⏱️ 2分钟

1. 点击导航栏的 **"Project Summary"** 按钮
2. **重要**！确认数据状态全部是 ✓：
   ```
   ✓ 负荷数据已上传      ← 必须是绿色勾号
   ✓ 质量报告已生成      ← 必须是绿色勾号
   ✓ 储能测算已完成      ← 必须是绿色勾号
   ```
3. 填写项目信息：
   ```
   项目名称: 测试储能项目
   项目地点: 安徽省合肥市
   评估周期: (自动填充，来自负荷数据)
   ```
4. 点击 **"生成项目评估报告"** 按钮
5. 等待生成（10-30秒）

**预期结果**: 报告中包含实际数值，例如：
```markdown
### 1.2 核心评估结论
- **首年总收益：** 约 45.50 万元       ← 不是0！
- **等效年循环次数：** 约 280.0 次/年  ← 不是0！
- **平均负荷：** 约 1234.56 kW        ← 不是0！
```

## 🐛 调试日志位置

### 查看前端日志

1. 按 **F12** 打开开发者工具
2. 切换到 **Console** 标签
3. 生成报告时会看到：
   ```
   🔍 [buildLoadProfile] loadMeta: { avg_load_kw: 1234.56, ... }
   ✅ [buildLoadProfile] 构建完成
   🔍 [buildStorageConfig] storageCyclesPayload: { ...}
   ✅ [buildStorageConfig] 构建完成
   📤 [handleGenerate] 发送请求
   ```

### 查看网络请求

1. 在开发者工具中切换到 **Network** 标签
2. 找到 `/api/deepseek/project-summary` 请求
3. 点击查看 **Payload**
4. 确认 `load_profile`、`storage_config`、`storage_results` 有数据

### 查看后端日志

在运行 `uvicorn` 的终端窗口查看：
```
INFO:deepseek_summary:🔍 [extract_summary_data] load_profile: {...}
INFO:deepseek_summary:✅ [extract_summary_data] 提取完成
```

## 📊 状态检查对照表

| 状态指示 | 含义 | 对报告的影响 |
|---------|------|------------|
| ○ 负荷数据未上传 | 您还没上传CSV文件 | 负荷相关数据全为0 |
| ✓ 负荷数据已上传 | 已上传并解析成功 | 负荷数据有实际值 |
| ○ 质量报告未生成 | loadQuality为null | 缺失数据统计为空 |
| ✓ 质量报告已生成 | 数据质量分析完成 | 显示缺失天数/小时数 |
| ○ 储能测算未完成 | 没运行Calculate | 储能相关数据全为0 |
| ✓ 储能测算已完成 | 计算结果可用 | 循环次数、收益有值 |

## ❓ 常见问题

### Q1: 我确实上传了数据，但状态还是 ○

**可能原因**:
- 上传后刷新了页面（数据在浏览器内存中，刷新会丢失）
- 上传过程出错（检查浏览器Console是否有错误）
- 文件格式不正确（必须包含 `timestamp` 和 `load` 列）

**解决**: 重新上传文件

### Q2: Storage Cycles 点击Calculate没反应

**可能原因**:
- 后端服务未运行
- 负荷数据未上传（测算依赖负荷数据）
- 参数填写有误

**解决**: 
1. 确认后端运行（访问 http://localhost:8000/docs）
2. 确认已上传负荷数据
3. 检查浏览器Console和Network标签

### Q3: 报告生成很慢（超过1分钟）

**可能原因**:
- DeepSeek API响应慢
- 网络连接问题
- 数据量太大

**解决**: 耐心等待，或检查后端日志是否有错误

### Q4: 报告中只有部分数据是0

这是正常的！如果：
- 只完成了步骤1-2（上传数据） → 储能相关数据为0
- 只完成了步骤3（测算） → 负荷相关数据为0
- 完成了全部步骤 → 所有数据都有值

## 🎯 验证清单

生成报告前，请确认：

- [ ] 后端服务正在运行（http://localhost:8000）
- [ ] 前端服务正在运行（http://localhost:5173）
- [ ] 已上传负荷数据文件
- [ ] Load Analysis 页面显示负荷曲线和统计数据
- [ ] 已运行储能测算（点击Calculate并等待完成）
- [ ] Storage Cycles 页面显示计算结果
- [ ] Project Summary 页面"数据状态检查"全部显示 ✓
- [ ] 已填写项目名称和地点
- [ ] 点击"生成项目评估报告"并等待完成

## 🚀 快速测试命令

```powershell
# 1. 确认服务运行
curl http://localhost:8000/docs      # 后端
curl http://localhost:5173           # 前端

# 2. 运行API诊断（验证后端功能）
python quick_diagnostic.py

# 3. 如果诊断通过，按照上面的4步操作即可
```

## 📞 获取帮助

如果按照上述步骤操作后仍有问题，请提供：

1. **数据状态截图**: Project Summary 页面的"数据状态检查"区域
2. **浏览器Console日志**: F12 → Console → 完整输出
3. **Network请求详情**: F12 → Network → `/api/deepseek/project-summary` 请求的 Payload 和 Response
4. **后端日志**: 运行 uvicorn 的终端输出
5. **操作步骤**: 您具体执行了哪些步骤

---

**创建时间**: 2025-11-24  
**适用版本**: v1.1  
**状态**: 问题已定位 - 需要先上传数据和运行测算
