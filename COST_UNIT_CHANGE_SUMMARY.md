# 运维成本和换电芯成本单位修改总结

## 修改概述

将**年运维成本**和**更换电芯成本**的输入单位从固定值（元/年、元）改为单位成本（元/Wh），并根据储能容量自动计算实际成本。

## 修改公式

### 年运维成本
- **输入单位**：元/Wh（单位成本）
- **计算公式**：实际年运维成本（万元）= 输入值 × 储能容量(kWh) ÷ 10
- **示例**：输入 0.2 元/Wh，容量 500kWh → 实际成本 = 0.2 × 500 ÷ 10 = **10.00万元**

### 更换电芯成本
- **输入单位**：元/Wh（单位成本）
- **计算公式**：实际成本（万元）= 输入值 × 储能容量(kWh) ÷ 10
- **示例**：输入 1.5 元/Wh，容量 500kWh → 实际成本 = 1.5 × 500 ÷ 10 = **75.00万元**

## 修改文件清单

### 后端文件

#### 1. `backend/schemas.py`
- **修改内容**：
  - 更新 `StorageEconomicsInput.annual_om_cost` 字段描述，说明新的单位和计算公式
  - 更新 `StorageEconomicsInput.cell_replacement_cost` 字段描述，说明新的单位和计算公式

#### 2. `backend/app.py`
- **修改内容**：
  - 在 `@router.post("/storage/economics")` 端点中添加单位转换逻辑
  - 在请求处理时，将用户输入的单位成本（元/Wh）转换为实际年成本（万元）
  ```python
  actual_annual_om_cost = (request.annual_om_cost * request.installed_capacity_kwh) / 10
  actual_cell_replacement_cost = (request.cell_replacement_cost * request.installed_capacity_kwh) / 10 if request.cell_replacement_cost else None
  ```
  - 更新 API 文档字符串说明新的单位和计算逻辑

### 前端文件

#### 3. `types.ts`
- **修改内容**：
  - 更新 `StorageEconomicsInput` 接口注释，说明 `annual_om_cost` 和 `cell_replacement_cost` 的新单位和计算公式

#### 4. `components/StorageEconomicsPage.tsx`
- **修改内容**：
  - 更新"年运维成本"输入框标签：从"年运维成本（元/年）"改为"年运维成本单位成本（元/Wh）"
  - 添加实时计算提示：当用户输入单位成本和容量时，显示"实际年运维成本：XX万元"
  - 更新"更换电芯成本"输入框标签：从"更换电芯成本（元）"改为"更换电芯成本单位成本（元/Wh）"
  - 添加实时计算提示：当用户输入单位成本和容量时，显示"实际更换成本：XX万元"
  - 移除旧的建议值计算逻辑（`suggestedOmCost`）

### 测试文件

#### 5. `test_economics.py`
- **修改内容**：
  - 更新测试数据，使用新的单位成本格式
  - 添加计算过程注释，说明如何从单位成本转换为实际成本
  - 添加两个测试用例的计算说明

## 验证结果

### 测试案例
- **输入参数**：
  - 首年收益：100,000 元
  - 储能容量：500 kWh
  - 年运维成本单位成本：0.2 元/Wh
  - 更换电芯成本单位成本：1.5 元/Wh（可选）

- **实际成本计算**：
  - 年运维成本：0.2 × 500 ÷ 10 = **10.00万元**
  - 更换电芯成本：1.5 × 500 ÷ 10 = **75.00万元**

### 前端显示验证 ✅
1. 输入框标签正确更新为"（元/Wh）"单位
2. 实时计算提示正确显示转换后的万元值
3. 年度明细表格中正确显示运维成本（0.20元表示为0.20万元）
4. 计算结果正确生成 IRR=22.23%, 回本期=4.14年等指标

### 后端验证 ✅
1. API 端点正确接收单位成本并转换为实际成本
2. 经济性计算基于转换后的实际成本进行
3. 返回结果中现金流数据准确反映了成本扣减

## 使用建议

### 数据输入建议
- **年运维成本单位成本**：建议范围 0.1～0.5 元/Wh
  - 取决于电池类型、维护方案等因素
  - 可参考行业数据进行选择

- **更换电芯成本单位成本**：建议范围 0.8～2.5 元/Wh
  - 对应电芯价格波动
  - 如不涉及电芯更换，可留空或设置为 0

### 计算精度
- 所有成本都自动转换为万元单位存储和计算
- 前端显示时保留两位小数
- 确保财务模型精度

## 相关文档

- 经济性测算需求文档：`docs/1130经济性测算需求.md`
- API 接口文档：在 `backend/app.py` 中的 `/api/storage/economics` 端点
