### **负荷数据完整性分析程序需求说明**



**一、 数据输入描述**

导入数据为近一年的电力负荷（或类似时间序列数据），数据结构包含日期时间信息及对应的负荷数值。

数据可能存在的列结构有两种形式：

1. **单列时间戳结构：** `日期时间, 负荷`
2. **多列时间戳结构：** `日期, 时间, 负荷`

**二、 分析目标**

对导入数据进行全面的完整性（Completeness）、有效性（Validity）及准确性（Accuracy）分析，核心目标是检查数据与标准日历（365天，01/01 - 12/31）的对齐情况，并识别出异常的负荷数值。

**三、 完整性分析报告内容（待输出项）**

程序应生成一份数据完整性分析报告，重点说明以下四类异常情况及其影响的日期或记录数量：

1. **日期/时间点缺失说明（缺失率分析）：**
   - **日历天缺失：** 统计与标准 365 天日历天（01/01 至 12/31）对比，哪些日期完全没有记录（即**整天数据丢失**）。
   - **时间点缺失：** 统计在有记录的日期中，缺少了多少预期的采样时间点（针对高频采样数据）。
2. **负荷为空（Null/NaN）说明：**
   - 统计并列出负荷（`负荷` 列）数据为空值（Null, NaN, 或空白字符串）的记录数量和占比。
3. **负荷为零（Zero）说明：**
   - 统计并列出负荷数值**严格等于 0** 的记录数量和占比。此项应特别指出**连续为 0** 的时间段，以便区分正常低负荷和数据采集中断。
4. **负荷为负值（Negative）说明：**
   - 统计并列出负荷数值**小于 0** 的异常记录数量和占比。

