多配置太复杂暂不增加配置，window_avg口径计算逻辑“跨午夜拼接”默认就是将日末配置传送到当日日初，如果后期开发step_15min（SOC 逐点积分）口径计算模式则会将日末配置传送到下一日的日初

Edited backend\app.py (+1 -1)
    173              merge_threshold_minutes=merge_threshold_minutes,
    174 -            wrap_across_midnight=False,
    174 +            wrap_across_midnight=True,

跨午夜拼接说明（按你的要求）

  - 目前 window_avg 已默认启用“首尾同类拼接”：23 点若与 0 点同类会连成一个窗口（你的
    c1 充 7 小时的场景）
  - 后续 step_15 口径我们会按“把日末配置传到下一日”（SOC 跨日结转）的语义实现，不与
    window_avg 混用
---
**sample、 physics 口径**
✅ physics 口径（物理真实能量流）

逻辑：电池是“真实容器”，效率和 DOD 影响从电池流向电网的能量。
这是程序当前使用的计算口径
🔹 特点：

以电池为中心计算，符合物理能量守恒；

体现能量流动损耗（η<1）；

通常用于 EMS / 物理仿真 / 实时功率计算。

✅ sample 口径（示例/手算简化）

逻辑：把电网侧作为基数，反推电池端的容量。
这是人工手算常见的“反向折算”方法。
🔹 特点：

以电网为中心计算；

多用于经济分析或手算估算；

对效率和 DOD 的处理方向与 physics 相反。



  术语澄清

  - window_debug 里的 base_kwh = allow_kw × hours 是“电池侧基数”（窗口平均 × 时长），不是“电网侧可充/放电量”；
  - “可充/放电量”= base_kwh 经过能量公式折算到“电网侧”，两种口径不同：
    - physics：充= base×DOD/η；放= base×DOD×η（程序当前使用）
    - sample：充= base/ DOD×η；放= base/ DOD/η（你手算使用）

  - ops_by_hour
      - 每行一日，列为 date, h00..h23（24 列），值是最终用于计算的运行逻辑
        （'充'/'放'/'待机'）。
      - 用法：一眼查看某日每小时的归属，核对是否与月配置/日期规则一致。
  - runs_debug
      - 按发生顺序列出当日“连段”（不跨日的同类连续小时块）：
          - date, seq（当日序号）, kind（'充'/'放'）, start_hour, end_hour（半开区间）, length_hours
          - filtered_by_threshold（是否因阈值被过滤）
          - merged_to（'c1'/'c2'/None）
          - wrap_across_midnight（本日是否启用了首尾合并）
      - 用法：复盘“连段”如何被阈值过滤/合并到 c1/c2，解释为何 11/23 被纳入 c2 充等现象。

---
下面按列逐一说明 window_debug 报表字段的含义、单位与计算口径。每行代表某一天（date）
  中的一个“窗口”（window=c1/c2 × kind=charge/discharge）的汇总结果。

  - date
      - 含义：所属自然日（YYYY‑MM‑DD）
      - 口径：以自然日为边界，window_avg 已启用“跨午夜拼接（首尾同类合并）”
  - window
      - 含义：窗口归属。c1 表示“第 1 次循环”，c2 表示“第 2 次循环”
      - 规则：当日顺序提取“充/放”连段，前两段归入 c1，其后同类连段并入 c2
  - kind
      - 含义：窗口类型。charge=充电窗口，discharge=放电窗口
  - hour_list
      - 含义：该窗口包含的整点时段起始小时列表（0–23），每个小时代表 [h, h+1)
      - 例：0,1,2 表示 00:00–03:00 三个整点小时；23 表示 23:00–24:00
  - points
      - 含义：该窗口内 15 分钟采样点数量
      - 关系：points × 0.25 = hours
  - hours（h）
      - 含义：窗口时长（小时）
      - 计算：hours = points × 0.25（一个 15 分钟点算 0.25 小时）
  - limit_kw（kW）
      - 含义：许可功率上限的“天内使用值”
      - 来源：按口径取值
          - transformer_capacity：kVA × 功率因数
          - monthly_demand_max：当月（本次上传数据内）的 15 分钟最大需量（kW）
  - avg_load_kw（kW）
      - 含义：窗口内 15 分钟负荷（load_kw）的算术平均值
  - allow_kw（kW）
      - 含义：窗口“许可功率”（窗口平均法）
      - 计算：
          - 充电：allow_ch = max(limit_kw − reserve_charge_kw − avg_load_kw, 0)
          - 放电：allow_dis = max(avg_load_kw − reserve_discharge_kw, 0)
      - 注：窗口平均法下不受 Pmax（c_rate×capacity）限制
  - base_kwh（kWh）
      - 含义：电池侧基数能量（窗口平均法）
      - 计算：base_kwh = allow_kw × hours（窗口平均 × 时长）
  - **e_grid_kwh（kWh）**
      - 含义：电网侧能量（“主口径”，随页面选择的 energy_formula 而变）
      - 计算：
          - 若 energy_formula=physics：充=base×DOD/η，放=base×DOD×η
          - 若 energy_formula=sample：充=base/DOD×η，放=base/DOD/η
  - full_ratio
      - 含义：该窗口的“满充/满放率”（“主口径”，随 energy_formula 而变）
      - 计算：full_ratio = min(e_grid_kwh / capacity_kwh, 1)
  - e_grid_kwh_physics / full_ratio_physics
      - 含义：电网侧能量/满充率（“对拍列”−固定用 physics 口径计算）
      - 用途：用于和 physics 口径的手工核对（不影响主口径与次数）
  - e_grid_kwh_sample / full_ratio_sample
      - 含义：电网侧能量/满充率（“对拍列”−固定用 sample 口径计算）
      - 用途：用于和 sample 口径的手工核对（不影响主口径与次数）
      - 你贴的手工公式即为 sample 口径：充=base/DOD×η；放=base/DOD/η
  - base_kwh_step15（kWh）
      - 含义：电池侧基数能量（逐点积分对照；不是窗口平均）
      - 计算：对窗口内每个 15 分钟点 t 取 p_t= max(limit − reserve − load_t, 0)（充）
        或 max(load_t − reserve, 0)（放），再 Σ(p_t × 0.25h)
      - 注：因 max(·) 非线性，“逐点积分”一般 ≥ “窗口平均 × 时长”
  - e_grid_kwh_physics_step15 / full_ratio_physics_step15
      - 含义：逐点积分的电网侧能量/满充率（固定 physics 口径）
      - 计算：在 base_kwh_step15 基础上套用 physics 折算
  - e_grid_kwh_sample_step15 / full_ratio_sample_step15
      - 含义：逐点积分的电网侧能量/满充率（固定 sample 口径）
      - 计算：在 base_kwh_step15 基础上套用 sample 折算

---
报表 outputs\20251110_011155\points_payload_计算结果.xlsx中数据如下：
1. 没有 monthly_limit 工作表，max kw在qcsheet中，数值如下：
year_month	max_kw
2024-10	506.25
2024-11	898.35
2024-12	997.5
2025-01	979.65
2025-02	1082.7
2025-03	1145.25
2025-04	1126.95
2025-05	936.45
2025-06	928.5
2025-07	935.25
2025-08	988.8
2025-09	1029
2025-10	918.6
2. 没有qc_head 工作表，有的是qc工作表，其中limit_mode为monthly_demand_max
、transformer_limit_kw为空
3. 没有window_debug 工作表