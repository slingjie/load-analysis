1. 新建 `MonthlyAverageStackedChart.tsx` 组件（ECharts + 月份选择器）。
2. 数据聚合：按月(1-12)与小时(0-23)计算“日平均小时负荷”。
3. 集成到 `LoadAnalysisPage`，新增卡片区块与标题文案。
4. 调整章节编号，确保语义与顺序一致。
5. 手工验证：
   - 仅 1 个月数据、多月跨年、缺小时场景；
   - 单选与多选切换、全选/全不选；
   - ECharts 自适应与窗口缩放。

