 - [x] 后端接口骨架：新增 `POST /api/storage/cycles`，返回占位结构与 400/500 错误处理（验证：空/错入参得到清晰错误）。
 - [x] 负荷解析：支持 CSV/XLSX，归一化为 `timestamp, load_kw`，并完成日对齐与 15 分钟重采样（验证：`迁移任务方案/测试样例/样例_2025-01-01_96点.csv` 读入 96 点）。
 - [x] 需求口径统计：实现 `monthly_demand_max` 与 `transformer_capacity` 两种上限（验证：返回的 limit_kw 可复核）。
 - [x] 策略转掩码：从格子与日期规则生成充/放/待机片段，支持跨日合并与小片段阈值，超两次循环执行“影响最小合并”（验证：构造 6 段案例触发合并并在 QC 记录）。
 - [x] TOU 映射：基于当日档位与当月价格映射至 15 分钟点位，缺价点纳入 QC（已实现 `build_price_series`）。
 - [x] 计算实现：window_avg + physics 默认；输出日 cycles、月度汇总、年度累计（验证：样例日循环数与期望对拍）。
 - [x] 报表导出：Excel 单文件多 Sheet（结果、TOU 配置快照、缺失/缺价明细），输出至 `outputs/<ts>/...` 并返回相对路径（验证：可下载且 Sheet 完整）。
 - [x] 前端页面：新增 `StorageCyclesPage`（表单 + 上传 + 提交 + 图表 + 下载入口 + QC 徽标）；无新依赖（验证：开发环境加载成功）。
 - [x] 图表与统计：月次数柱状、日次数折线、头部年累计（已在 `StorageCyclesPage` 中实现）。
 - [ ] 对拍与回归：依据“验收要点与测试用例.md”覆盖跨午夜合并、多于两次循环、缺价/缺失、两种计费模式（验证：结果合理与稳定）。

对拍建议清单（按“验收要点与测试用例.md”）：
- 跨午夜合并：构造 23:00-24:00 与次日 00:00-02:00 同类（充/放），期望首尾合并并在 `qc.merged_segments` 计数合理；
- 多于两次循环：6 段充/放交替，期望收敛至 c1/c2，两次循环；
- 缺价处理：在某月价格映射为空（`null`）或非数值，期望 `qc.missing_prices` > 0 且计算链路不中断；
- 计费口径切换：`monthly_demand_max` 与 `transformer_capacity` 切换，结果变化合理；
- 性能：96×365 规模数据在秒级~十秒级内完成（视环境），Excel 可下载。
