 - [ ] 明确第一阶段边界：纯本地是否需要登录（默认不需要）、项目/数据集最小字段、点位序列化格式（JSONL/CSV）。
 - [ ] 本地存储实现：基于 IndexedDB（优先）实现 `projects/datasets` 的增删改查，并提供必要的 localStorage 降级策略。
 - [ ] 项目管理 UI：创建/选择项目（最小：下拉 + 新建弹窗），并默认落到本地存储。
 - [ ] 数据集本地保存：负荷分析结果可用时提供“保存到项目”，写入本地存储，展示成功/失败与重复提示（可选去重）。
 - [ ] 数据集列表与加载：按项目列出数据集，支持选择后加载到全局 `loadCleanedData/loadMeta/loadQuality`。
 - [ ] 测算引用：在 `StorageCyclesPage` / `StorageEconomicsPage` 等需要负荷输入的页面，优先使用已加载的数据集走 `payload.points`，避免重复上传。
 - [ ] 本地体验完善：删除/重命名、导入/导出（JSON）与异常降级提示；确保离线可用。
 - [ ] 手工回归：上传→保存→刷新→选择→复算；大数据集性能与失败降级验证。
 - [ ] 第二阶段（迁移到 Supabase）：接入 Supabase（Auth + 表 + Storage + RLS），并提供“本地→Supabase”迁移入口（向导/批量上传）。
 - [ ] 基础运维（第二阶段）：补充 README（Supabase 环境变量、RLS 配置步骤、Cloudflare 部署注意事项、CORS/安全建议）。
