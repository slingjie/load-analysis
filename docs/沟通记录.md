### 未完成测试

- [ ] 12-05storage cycles页面的 是否能增加收益率或者收益年限的对比，从充放次数和收益综合来判断

- [ ] 有个使用流程问题，我们来讨论。当前“storage cycles”页面中“批量容量对比测试”计算的前提是需要设置好本页面中的储能相关参数中除了容量以外的所有参数（包括充放电功率、效率、SOC 范围、充放电时段等），否则无法进行合理对比。请优化这个设置流程，给出方案我们来讨论，可以重构当前的储能常规参数设置和高级参数设置的交互流程和逻辑。配合“批量容量对比测试”功能，提升用户体验。

批量对比区方案：
┌────────────────────────────────────────────────────────────────────────┐
│  📊 批量容量对比测算                                                    │
├────────────────────────────────────────────────────────────────────────┤
│ 容量范围: 261 ~ 1566 kWh (步长 261, 共6档)    目标循环: 640 次  [计算]  │
├──────────────────────────────────┬─────────────────────────────────────┤
│          对比表格                 │           趋势图                    │
│  ┌────────────────────────────┐  │   循环 ▲                            │
│  │ 容量    循环    收益    选择│  │   720 ┤ ●                           │
│  │ 261    701.39   7.5万   ○  │  │       │   ●                         │
│  │ 522    687.57  14.6万   ○  │  │   680 ┤     ●                       │
│  │ 783    673.03  21.4万   ○  │  │       │       ●                     │
│  │ 1044   656.23  27.8万   ○  │  │   640 ┼─────────●─── 目标线        │
│  │ 1305   641.16  33.9万   ⬤  │  │       │           ●                 │
│  │ 1566   624.85  39.7万   ○  │  │   600 ┤             ●               │
│  └────────────────────────────┘  │       └──┬──┬──┬──┬──┬──► 容量      │
│                                  │         261 522 ...  1566           │
└──────────────────────────────────┴─────────────────────────────────────┘

### 满放率计算
`iF( 第一次可放电量<0,0,IF( 第一次可放电量="无数据/数据缺失",0%,IF( 第一次可放电量>额定容量*第一次满充率 ,第一次满充率 ,第一次可放电量*第一次满充率/额定容量 )))`
IF(可放电量 < 0, 0, 
   IF(可放电量 = "无数据/数据缺失", 0%, 
      IF(可放电量 > 额定容量 × 满充率, 
         满充率,  // 放电量超过充电量时，满放率上限为满充率
         (可放电量 × 满充率) / 额定容量  // 正常情况
      )
   )
)

总结一句

你问我“请遍历程序查看需要确认的内容”，我已经把跟你手算相关的所有关键参数（电池容量、变压器容量、DOD、效率、充放余量、充放时段、满充/满放率计算方式）在代码里全部对上了。
代码里确实是用这些参数在算窗口平均功率、窗口能量、满充率/满放率，并通过 window_month_summary 汇总给前端。
现在“第二次放电满放率是对的，第一次不对”基本可以确定是：C1 这组窗口在某天的“窗口选择/平均功率/小时数”与 Excel 你手工选的区间略有差别，而不是“参数没有从系统里传进去”。
下一步，你可以选择：

要么把你导出的 window_debug 里那天 C1 的两行贴给我，我帮你把“程序内算值 vs 你 Excel 手算”逐列对上；
要么我按你 Excel 的公式，在后端 _window_metrics 里新添一套“严格对 Excel 的计算栏”（带详细中文注释），你来对拍一遍我们是否已经 1:1 还原。